

### 📄 محتویات فایل `README.md`

```markdown
# 🤖 ربات تلگرام اعلانات پرداخت و مدیریت کاربران (نسخه PHP)

یک ربات هوشمند و سبک برای مدیریت کاربران، دریافت رسیدهای تصویری و ارسال اعلانات خودکار ماهانه، طراحی شده برای اجرا روی هاست‌های اشتراکی.

---

## 🌟 قابلیت‌ها

* **اعلان خودکار (Cron Job):** ارسال پیام یادآوری خودکار به تمام کاربران در روزهای ۲۸ و ۲۹ هر ماه.
* **ارسال همگانی:** توانایی ارسال پیام توسط ادمین به تمام اعضای ربات.
* **ارسال اختصاصی:** ارسال پیام از طرف ادمین به یک کاربر خاص با استفاده از آیدی عددی.
* **سیستم دریافت رسید:** کاربران تنها با زدن دستور `/photo` مجاز به ارسال عکس هستند (جلوگیری از اسپم).
* **گزارش‌دهی کامل:** نمایش لیست کاربران همراه با **نام**، **آیدی عددی** و **یوزرنیم** با دستور `/users`.
* **آمار لحظه‌ای:** اطلاع‌رسانی به ادمین بلافاصله پس از عضویت کاربر جدید.
* **کپی سریع:** نمایش آیدی کاربران به صورت `Mono` جهت کپی و استفاده سریع ادمین.

---

## 🛠 دستورات ربات

### دستورات عمومی:
* `/start` - شروع کار با ربات و ثبت‌نام در لیست اعلانات.
* `/photo` - شروع فرآیند ارسال عکس رسید.
* `/cancel` - لغو عملیات فعلی (ارسال عکس یا پیام).

### دستورات ادمین:
* `/users` - مشاهده لیست کامل کاربران با جزئیات.
* `/send [Message]` - ارسال پیام به همه کاربران.
* `/send [UserID] [Message]` - ارسال پیام به کاربر خاص.
    * *مثال:* `/send 12345678 سلام، رسید شما تایید شد.*

---

## 🚀 نصب و راه‌اندازی

### ۱. پیش‌نیازها
* یک هاست با پشتیبانی از PHP.
* توکن ربات تلگرام (دریافت شده از @BotFather).
* آیدی عددی ادمین.

### ۲. آپلود فایل‌ها
فایل‌های زیر را در یک پوشه (مثلاً `telegram-bot`) آپلود کنید:
* `index.php`: هسته اصلی ربات.
* `notify.php`: فایل مخصوص اعلانات خودکار.
* `users.txt`: فایل ذخیره آیدی کاربران (دسترسی فایل روی 644 یا 755 باشد).

### ۳. ست کردن وب‌هوک (Webhook)
برای اتصال ربات به تلگرام، آدرس زیر را در مرورگر خود باز کنید (اطلاعات خود را جایگزین کنید):
`https://api.telegram.org/bot[YOUR_TOKEN]/setWebhook?url=https://yourdomain.com/telegram-bot/index.php`

### ۴. تنظیم کرون‌جاب (Cron Job)
برای ارسال پیام‌های خودکار در روزهای ۲۸ و ۲۹ ماه، در پنل هاست (Cpanel/DirectAdmin) یک Cron Job با تنظیمات زیر بسازید:

* **زمان:** `30 08 * * *` (ساعت ۸:۳۰ صبح هر روز)
* **دستور (Command):**
```bash
/usr/local/bin/php /home/username/public_html/telegram-bot/notify.php > /dev/null 2>&1

```

---

## ⚙️ شخصی‌سازی

شما می‌توانید پیام‌های خوش‌آمدگویی، متن اعلانات و زمان‌بندی را در دو فایل زیر به راحتی تغییر دهید:

1. **`index.php`**: تغییر توکن، آیدی ادمین و پیام‌های پاسخ‌دهی.
2. **`notify.php`**: تغییر روزهای ارسال اعلان و متن یادآوری.

---

## 🛡 امنیت

فایل `users.txt` حاوی آیدی کاربران است. برای امنیت بیشتر پیشنهاد می‌شود دسترسی مستقیم به این فایل را از طریق فایل `.htaccess` محدود کنید.

```
